import React from 'react';

import { Layout } from '../components/layout';

const HomePage = () => {
  console.log('home');
  return (
    <Layout>
      HOME
    </Layout>
  );
};

export default HomePage;
